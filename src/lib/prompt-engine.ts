type PromptService = { title: string; promptTemplate: string };
type PromptFile = { originalName: string; fileUrl: string; fileType: string };
export function generateDesignPrompt({ service, briefData, files = [], notes }: { service: PromptService; briefData: Record<string, unknown>; files?: PromptFile[]; notes?: string }) {
  const rows = Object.entries(briefData).filter(([, v]) => v !== undefined && v !== '').map(([k, v]) => `- ${k}: ${Array.isArray(v) ? v.join(', ') : String(v)}`).join('\n') || '- Данные не заполнены';
  const refs = files.length ? files.map((f) => `- ${f.originalName} (${f.fileType}): ${f.fileUrl}`).join('\n') : '- Референсы не загружены';
  return `# Промт для AI + Human дизайна\n\n## 1. Тип услуги\n${service.title}\n\n## 2. Цель дизайна\n${service.promptTemplate}\n\n## 3. Данные пользователя\n${rows}\n\n## 4. Технические требования\nСоблюдать формат площадки, читаемость текста, аккуратную композицию и коммерческую применимость.\n\n## 5. Целевая аудитория\nИспользовать данные из брифа: targetAudience / аудитория / ниша.\n\n## 6. Стиль\nОпираться на поля style, colors и бренд-материалы клиента.\n\n## 7. Цвета\nИспользовать указанные цвета; если данных мало — предложить гармоничную палитру.\n\n## 8. Тексты\nТексты должны быть читаемыми, без артефактов и случайных символов.\n\n## 9. Референсы и файлы\n${refs}\n\n## 10. Ограничения\nНе использовать запрещённые элементы, избегать перегруженности, не имитировать чужие товарные знаки.\n\n## 11. Количество вариантов\nПодготовить 3 сильных направления/варианта для отбора оператором.\n\n## 12. Главная инструкция\nСделать прикладной коммерческий дизайн, а не случайную нейросетевую картинку.\n\n## 13. Контроль качества\nПроверить читаемость текста, визуальную иерархию, композицию, соответствие задаче и готовность к дальнейшей ручной доработке.${notes ? `\n\n## Заметки оператора\n${notes}` : ''}`;
}

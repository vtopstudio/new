import Link from 'next/link';
import { money, orderStatus, paymentStatus } from '@/lib/format';
export function Shell({ title, subtitle, children }: { title: string; subtitle?: string; children: React.ReactNode }) { return <div className="mx-auto max-w-6xl px-4 py-10"><div className="mb-8"><h1 className="text-4xl font-black tracking-tight">{title}</h1>{subtitle && <p className="mt-3 max-w-3xl text-slate-300">{subtitle}</p>}</div>{children}</div>; }
export function ServiceCard({ service }: { service: { slug: string; title: string; shortDescription: string; basePrice: unknown } }) { return <div className="card flex flex-col"><h3 className="text-2xl font-bold">{service.title}</h3><p className="mt-3 flex-1 text-slate-300">{service.shortDescription}</p><p className="mt-5 text-xl font-black">от {money(service.basePrice)}</p><div className="mt-5 flex gap-3"><Link className="btn" href={`/order/new/${service.slug}`}>Заказать</Link><Link className="btn-secondary" href={`/services/${service.slug}`}>Подробнее</Link></div></div>; }
export function StatusPill({ value, type='order' }: { value: string; type?: 'order'|'payment' }) { return <span className="rounded-full bg-white/10 px-3 py-1 text-xs">{type==='order' ? orderStatus[value] : paymentStatus[value]}</span>; }

import { getServerSession } from "next-auth";import { redirect } from "next/navigation";import { authOptions } from "@/lib/auth";import { prisma } from "@/lib/db";
export default async function Balance(){const s=await getServerSession(authOptions);if(!s?.user)redirect("/auth/sign-in");const u=await prisma.user.findUnique({where:{id:(s.user as any).id}});return <main className="mx-auto max-w-3xl px-4 py-10"><div className="card p-8"><h1 className="text-3xl font-black">Баланс</h1><p className="mt-6 text-4xl font-black text-indigo-700">{u?.balance.toString()} руб.</p><p className="mt-3 text-slate-600">В MVP баланс отображается справочно. Основной способ оплаты — YooKassa по каждому заказу.</p></div></main>}

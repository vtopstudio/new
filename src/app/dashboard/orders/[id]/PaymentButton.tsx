"use client";import { useState } from "react";export function PaymentButton({orderId}:{orderId:string}){const[loading,setLoading]=useState(false);async function pay(){setLoading(true);const res=await fetch("/api/payments/yookassa/create",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({orderId})});const data=await res.json();setLoading(false);if(data.confirmationUrl) location.href=data.confirmationUrl; else alert(data.error||"Не удалось создать платеж")}return <button onClick={pay} disabled={loading} className="btn">{loading?"Создаём платёж...":"Оплатить через YooKassa"}</button>}

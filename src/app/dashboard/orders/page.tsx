import Link from "next/link";import { getServerSession } from "next-auth";import { redirect } from "next/navigation";import { authOptions } from "@/lib/auth";import { prisma } from "@/lib/db";import { StatusBadge } from "@/components/StatusBadge";
export default async function Orders(){const s=await getServerSession(authOptions);if(!s?.user)redirect("/auth/sign-in");const orders=await prisma.order.findMany({where:{userId:(s.user as any).id},include:{service:true},orderBy:{createdAt:"desc"}});return <main className="mx-auto max-w-5xl px-4 py-10"><h1 className="text-4xl font-black">Мои заказы</h1><div className="mt-6 space-y-3">{orders.map(o=><Link className="card flex justify-between p-5" href={`/dashboard/orders/${o.id}`} key={o.id}><span>{o.service.title}</span><StatusBadge value={o.status}/></Link>)}</div></main>}

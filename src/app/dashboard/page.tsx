import Link from "next/link";import { getServerSession } from "next-auth";import { redirect } from "next/navigation";import { authOptions } from "@/lib/auth";import { prisma } from "@/lib/db";import { StatusBadge } from "@/components/StatusBadge";
export default async function Dashboard(){const session=await getServerSession(authOptions);if(!session?.user)redirect("/auth/sign-in");const orders=await prisma.order.findMany({where:{userId:(session.user as any).id},include:{service:true},orderBy:{createdAt:"desc"},take:5});return <main className="mx-auto max-w-6xl px-4 py-10"><h1 className="text-4xl font-black">Личный кабинет</h1><div className="mt-6 grid gap-5 md:grid-cols-3"><Link className="card p-6" href="/services"><b>Новый заказ</b><p className="mt-2 text-slate-600">Выбрать услугу</p></Link><Link className="card p-6" href="/dashboard/orders"><b>Мои заказы</b><p className="mt-2 text-slate-600">История и файлы</p></Link><Link className="card p-6" href="/dashboard/profile"><b>Профиль</b><p className="mt-2 text-slate-600">Данные аккаунта</p></Link></div><h2 className="mt-10 text-2xl font-black">Последние заказы</h2><div className="mt-4 space-y-3">{orders.map(o=><Link href={`/dashboard/orders/${o.id}`} key={o.id} className="card flex items-center justify-between p-5"><div><b>{o.service.title}</b><p className="text-sm text-slate-500">#{o.id}</p></div><StatusBadge value={o.status}/></Link>)}</div></main>}

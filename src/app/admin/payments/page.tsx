import { getServerSession } from "next-auth";import { redirect } from "next/navigation";import { StatusBadge } from "@/components/StatusBadge";import { authOptions,isAdmin } from "@/lib/auth";import { prisma } from "@/lib/db";
export default async function AdminPayments(){const s=await getServerSession(authOptions);if(!isAdmin(s))redirect("/dashboard");const payments=await prisma.payment.findMany({include:{user:true,order:true},orderBy:{createdAt:"desc"}});return <main className="mx-auto max-w-6xl px-4 py-10"><h1 className="text-4xl font-black">Платежи YooKassa</h1><div className="mt-6 space-y-3">{payments.map(p=><div className="card grid gap-3 p-5 md:grid-cols-5" key={p.id}><span>{p.user.email}</span><span>{p.amount.toString()} руб.</span><StatusBadge value={p.status}/><span>{p.providerPaymentId}</span><span>{p.createdAt.toLocaleString("ru-RU")}</span></div>)}</div></main>}

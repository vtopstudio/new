import Link from "next/link";import { prisma } from "@/lib/prisma";import { orderStatusLabels,paymentStatusLabels,rub } from "@/lib/format";
export const dynamic="force-dynamic";
export default async function AdminOrders(){const orders=await prisma.order.findMany({include:{service:true,user:true,payments:true},orderBy:{createdAt:"desc"}});return <main className="mx-auto max-w-7xl px-4 py-12"><h1 className="text-4xl font-black">Заказы</h1><div className="mt-8 space-y-4">{orders.map(o=><Link className="card block" href={`/admin/orders/${o.id}`} key={o.id}><div className="flex flex-wrap justify-between gap-3"><div><b>{o.service.title}</b><p className="text-sm text-slate-500">{o.user.email} · №{o.id}</p></div><div className="flex gap-2"><span className="badge">{orderStatusLabels[o.status]}</span><span className="badge">{paymentStatusLabels[o.paymentStatus]}</span><b>{rub(o.price)}</b></div></div></Link>)}</div></main>}

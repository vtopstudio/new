import Link from "next/link";import { getServerSession } from "next-auth";import { redirect } from "next/navigation";import { authOptions,isAdmin } from "@/lib/auth";import { prisma } from "@/lib/db";import { StatusBadge } from "@/components/StatusBadge";
export default async function AdminOrders(){const s=await getServerSession(authOptions);if(!isAdmin(s))redirect("/dashboard");const orders=await prisma.order.findMany({include:{service:true,user:true},orderBy:{createdAt:"desc"}});return <main className="mx-auto max-w-6xl px-4 py-10"><h1 className="text-4xl font-black">Все заказы</h1><div className="mt-6 space-y-3">{orders.map(o=><Link className="card grid gap-3 p-5 md:grid-cols-4" href={`/admin/orders/${o.id}`} key={o.id}><span>{o.service.title}</span><span>{o.user.email}</span><StatusBadge value={o.status}/><span>{o.price.toString()} руб.</span></Link>)}</div></main>}

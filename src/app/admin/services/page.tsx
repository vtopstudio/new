import { getServerSession } from "next-auth";import { redirect } from "next/navigation";import { authOptions,isAdmin } from "@/lib/auth";import { prisma } from "@/lib/db";
export default async function AdminServices(){const s=await getServerSession(authOptions);if(!isAdmin(s))redirect("/dashboard");const services=await prisma.service.findMany({orderBy:{createdAt:"asc"}});return <main className="mx-auto max-w-6xl px-4 py-10"><h1 className="text-4xl font-black">Услуги</h1><div className="mt-6 grid gap-5 md:grid-cols-2">{services.map(x=><div className="card p-6" key={x.id}><h2 className="text-xl font-black">{x.title}</h2><p>{x.description}</p><p className="mt-3 font-black">{x.basePrice.toString()} руб.</p><p className="text-sm text-slate-500">slug: {x.slug}</p></div>)}</div></main>}

import { Shell } from '@/components/ui';
export default function SignUp({ searchParams }: { searchParams: { error?: string } }) { return <Shell title="Регистрация"><form className="card max-w-md" action="/api/auth/sign-up" method="post"><label className="label">Имя</label><input className="input" name="name" required /><label className="label mt-4">Email</label><input className="input" name="email" type="email" required /><label className="label mt-4">Пароль</label><input className="input" name="password" type="password" minLength={8} required />{searchParams.error && <p className="mt-3 text-red-300">{searchParams.error}</p>}<button className="btn mt-6">Создать аккаунт</button></form></Shell>; }

"use client";
import Link from "next/link";import { signIn } from "next-auth/react";import { useRouter } from "next/navigation";import { FormEvent,useState } from "react";
export default function SignUp(){const r=useRouter();const[err,setErr]=useState("");async function onSubmit(e:FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);const body={name:fd.get("name"),email:fd.get("email"),password:fd.get("password")};const res=await fetch("/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});if(!res.ok){setErr((await res.json()).error||"Ошибка регистрации");return}await signIn("credentials",{email:body.email,password:body.password,redirect:false});r.push("/dashboard")}return <main className="mx-auto max-w-md px-4 py-12"><form onSubmit={onSubmit} className="card p-8"><h1 className="text-3xl font-black">Регистрация</h1>{err&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-red-700">{err}</p>}<label className="label mt-5">Имя</label><input className="input" name="name" required/><label className="label mt-4">Email</label><input className="input" name="email" type="email" required/><label className="label mt-4">Пароль</label><input className="input" name="password" type="password" minLength={6} required/><button className="btn mt-6 w-full">Создать аккаунт</button><p className="mt-4 text-sm">Уже есть аккаунт? <Link className="text-indigo-700" href="/auth/sign-in">Войти</Link></p></form></main>}

import { Shell } from '@/components/ui';
export default function SignIn({ searchParams }: { searchParams: { error?: string } }) { return <Shell title="Вход"><form className="card max-w-md" action="/api/auth/sign-in" method="post"><label className="label">Email</label><input className="input" name="email" type="email" required /><label className="label mt-4">Пароль</label><input className="input" name="password" type="password" required />{searchParams.error && <p className="mt-3 text-red-300">{searchParams.error}</p>}<button className="btn mt-6">Войти</button></form></Shell>; }

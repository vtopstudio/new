"use client";
import Link from "next/link";import { signIn } from "next-auth/react";import { useRouter } from "next/navigation";import { FormEvent,useState } from "react";
export default function SignIn(){const r=useRouter();const[err,setErr]=useState("");async function onSubmit(e:FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);const res=await signIn("credentials",{email:fd.get("email"),password:fd.get("password"),redirect:false});if(res?.ok)r.push("/dashboard");else setErr("Неверный email или пароль")}return <main className="mx-auto max-w-md px-4 py-12"><form onSubmit={onSubmit} className="card p-8"><h1 className="text-3xl font-black">Вход</h1>{err&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-red-700">{err}</p>}<label className="label mt-5">Email</label><input className="input" name="email" type="email" required/><label className="label mt-4">Пароль</label><input className="input" name="password" type="password" required/><button className="btn mt-6 w-full">Войти</button><p className="mt-4 text-sm">Нет аккаунта? <Link className="text-indigo-700" href="/auth/sign-up">Зарегистрироваться</Link></p></form></main>}
